package com.huanyu_mod.world.dimension;

import com.huanyu_mod.HuanYuMod;
import net.minecraft.core.registries.Registries;
import net.minecraft.world.level.dimension.DimensionType;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.neoforge.registries.DeferredHolder;
import net.neoforged.neoforge.registries.DeferredRegister;

public class _ModDimensions {
    /*public static final DeferredRegister<DimensionType> DR_DIMENSIONTYPE = DeferredRegister.create(Registries.DIMENSION_TYPE, HuanYuMod.MOD_ID);

    public static final DeferredHolder<DimensionType, DimensionType> DIMENSION_EDITOR_INTERFACE =
            DR_DIMENSIONTYPE.register("dimension_editor_interface", () -> ? );

    public static void register(IEventBus eventBus) {
        DR_DIMENSIONTYPE.register(eventBus);
    }*/
}
